#include<iostream>
#include<vector>
#include<queue>
using namespace std;

void BFS(int start, vector<vector<int>>& graph, vector<int>& visit_order)
{
	queue<int>
}

int main(void)
{
	cin.tie(0); cout.tie(0);
	ios_base::sync_with_stdio(false);

	int N, M, R;
	cin >> N >> M >> R;
	if (N < 5 || N>100000 || M < 1 || M>200000 || R<1 || R>N)
	{
		return 0;
	}




	return 0;
}