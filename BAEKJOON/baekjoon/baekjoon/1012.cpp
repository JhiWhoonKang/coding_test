#include <iostream>
#include <vector>

using namespace std;

int main(void)
{
	cin.tie(0); cout.tie(0);
	ios_base::sync_with_stdio(false);

	int T, M, N, K; //T: 테스트 케이스 개수, M: 가로, N: 세로, K: 배추 개수
	pair<int, int> coor;


	cin >> T;
	cin >> M >> N >> K;

	for (int i = 0; i < K; i++)
	{
		int x, y;
		cin >> x >> y;

	}

	return 0;
}